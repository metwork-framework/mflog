pipeline:
  build:
    image: python:${PYTHON_VERSION}
    commands:
      - pip install -r test-requirements.txt
      - python setup.py install
      - flake8
      - pytest

matrix:
  PYTHON_VERSION:
    - 2.7-wheezy
    - 3.5-jessie
    - 3.6-jessie
